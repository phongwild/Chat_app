name: demo_app
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_auth: ^5.2.0
  firebase_core: ^3.7.0
  cloud_firestore: ^5.2.0
  firebase_database: ^11.2.0
  provider: ^6.1.2
  firebase_messaging: ^15.1.6
  googleapis_auth: ^1.6.0
  flutter_local_notifications: ^18.0.1
  permission_handler: ^11.3.1
  toastification: ^2.3.0
  # path_provider: ^2.1.5
  # shared_preferences: ^2.3.4
  #image_picker: ^1.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/google_service/account_service.json
